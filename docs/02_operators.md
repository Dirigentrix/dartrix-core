# Operators

AladdinLamp exposes `touch`, `whisper`, `apply_resonance`, `djinn`, and `operate`. Touch creates state, Whisper adds operator context, Resonance enforces the 108 Hz metadata contract, and Djinn returns either normalized output or a safe gate message.
